# docs

`demo.gif` is generated from [`../demo.tape`](../demo.tape) with [VHS](https://github.com/charmbracelet/vhs):

```bash
vhs demo.tape   # run from the repo root; writes docs/demo.gif
```

Commit the regenerated `demo.gif` whenever the UI changes.
